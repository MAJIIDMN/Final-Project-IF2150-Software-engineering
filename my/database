# con = sqlite3.connect("src/database/growbak.db")
# cursor = con.cursor()

# nama = ["boneka", "sapu", "tas", "botol minum", "topi", "kacamata hitam", "sepatu olahraga", "jaket", "headphone", "power bank", "flashdisk", "mouse wireless", "keyboard mechanical", "smartwatch", "kamera aksi"]
# id = ["H001", "H002", "H003", "H004", "H005", "H006", "H007", "H008", "H009", "H010", "H011", "H012", "H013", "H014", "H015"]

# for i in range(len(nama)):
#     foto_path = f"src/database/image/pointmart/{nama[i].replace(' ', '_')}.jpg"
#     query = "UPDATE hadiah SET foto_path = ? WHERE id = ?"
#     cursor.execute(query, (foto_path, id[i]))
#     con.commit()

# con.close()